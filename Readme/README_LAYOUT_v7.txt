IPPGOnline layout v7

- Legal footer raised to 10px from the page edge.
- Legal type has a 6pt minimum/current size and a taller stacked footer box.
- Free Mobile is no longer in the promo flex row. It is centered as the last promotional line before the footer.
- The plans area is now a fixed 520 x 255px frame. 2-, 3-, and 4-speed layouts divide that frame into equal rows, so plan content scales/flows inside a stable composition instead of relying on hard-coded row heights.
- Schedule text is no longer clipped by the old 42px max-height.
