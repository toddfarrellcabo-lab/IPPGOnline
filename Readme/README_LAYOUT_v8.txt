IPPGOnline layout v8

Builder
- Typography/spacing sliders now have an explicit Update Preview button.
- Slider values can be adjusted first, then applied together.
- This avoids the impression that a partially changed group of controls is the final layout.

Variable layout
- Plan/pricing content remains inside a fixed left-side frame, clear of the dog.
- Promo elements are left aligned and stacked vertically in a 330px-wide safe zone.
- Promo stack has a maximum height so it cannot continue down over the dog/footer.
- Free Mobile is treated separately from the promo stack and enlarged to 360px wide.
- Mobile is centered as the final callout above the footer.

Legal
- Legal remains at least 6pt.
- Legal is inset 10px from the bottom and given a taller footer frame.
